// Loading.js
import React from 'react';
import './Styles/LoadingRing.css'; // Import the CSS for the loading ring

const Loading = () => (
  <div className="loading-spinner">
    <div className="spinner"></div>
  </div>
);

export default Loading;
